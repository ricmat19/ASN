import React, { FC } from "react";
import StripeC from "../components/stripeComponent";

const StripeR: FC = () => {
  return (
    <div>
      <StripeC />
    </div>
  );
};

export default StripeR;
