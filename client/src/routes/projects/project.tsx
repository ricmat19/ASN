import React, { FC } from "react";
import ProjectC from "../../components/projects/project";

const ProjectR: FC = () => {
  return (
    <div>
      <ProjectC />
    </div>
  );
};

export default ProjectR;
