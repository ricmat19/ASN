import React, { FC } from "react";
import CheckoutC from "../../components/cart/checkout";

const CheckoutR: FC = () => {
  return (
    <div>
      <CheckoutC />
    </div>
  );
};

export default CheckoutR;
