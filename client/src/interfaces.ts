export interface Cart {
  cart: any[]
  qty: any
}
  
export interface Product {
    id: string
    title: string
    product: string
    price: string
    info: string
    imagekey: string
    qty: number
    primaryimage: boolean
}